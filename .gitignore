# Ignore all files by default
*

# Don't ignore .gitignore itself
!.gitignore

# Allow .txt, .yaml, and .py files
!*.txt
!*.yaml
!*.py

# Allow any file starting with "Dockerfile"
!Dockerfile*

# Optionally, you can also allow directories you want to track
!/

# If you want to explicitly allow certain directories (e.g., "src" or "config")
!src/
!config/
